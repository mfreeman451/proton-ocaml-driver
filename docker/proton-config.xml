<?xml version="1.0"?>
<proton>
    <!-- Basic server configuration for testing -->
    <logger>
        <level>error</level>
        <console>true</console>
        <log>/var/log/proton-server/proton-server.log</log>
        <errorlog>/var/log/proton-server/proton-server.err.log</errorlog>
        <size>1000M</size>
        <count>10</count>
    </logger>

    <!-- Network interfaces -->
    <listen_host>0.0.0.0</listen_host>

    <!-- HTTP interface -->
    <http_port>8123</http_port>

    <!-- Native TCP interface -->
    <tcp_port>8463</tcp_port>

    <!-- HTTPS interface (optional, for TLS testing) -->
    <https_port>8443</https_port>
    
    <!-- Native TCP with TLS (optional) -->
    <tcp_port_secure>9440</tcp_port_secure>

    <!-- Interserver communication -->
    <interserver_http_port>9009</interserver_http_port>

    <!-- Memory settings for testing -->
    <max_server_memory_usage>1610612736</max_server_memory_usage> <!-- 1.5GB -->
    <max_memory_usage_for_user>1073741824</max_memory_usage_for_user> <!-- 1GB -->

    <!-- Storage configuration -->
    <path>/var/lib/proton/</path>
    <tmp_path>/var/lib/proton/tmp/</tmp_path>
    <user_files_path>/var/lib/proton/user_files/</user_files_path>
    <format_schema_path>/var/lib/proton/format_schemas/</format_schema_path>

    <!-- Users configuration -->
    <users_config>users.xml</users_config>

    <!-- Default profile settings -->
    <profiles>
        <default>
            <max_memory_usage>1073741824</max_memory_usage>
            <use_uncompressed_cache>0</use_uncompressed_cache>
            <load_balancing>random</load_balancing>
        </default>
    </profiles>

    <!-- Default database -->
    <default_database>default</default_database>

    <!-- Timezone -->
    <timezone>UTC</timezone>

    <!-- Query log -->
    <query_log>
        <database>system</database>
        <table>query_log</table>
        <flush_interval_milliseconds>7500</flush_interval_milliseconds>
    </query_log>

    <!-- Disable some features not needed for testing -->
    <builtin_dictionaries_reload_interval>3600</builtin_dictionaries_reload_interval>
    <max_concurrent_queries>100</max_concurrent_queries>
    <max_thread_pool_size>10000</max_thread_pool_size>
    <max_connections>4096</max_connections>
    <keep_alive_timeout>3</keep_alive_timeout>
    
    <!-- For streaming queries -->
    <stream_poll_timeout_ms>500</stream_poll_timeout_ms>
    
    <!-- Enable compression for better testing -->
    <compression>
        <case>
            <min_part_size>10485760</min_part_size>
            <min_part_size_ratio>0.01</min_part_size_ratio>
            <method>lz4</method>
        </case>
    </compression>
</proton>